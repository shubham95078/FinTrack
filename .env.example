JWT_SECRET=change-me-to-a-long-random-secret
# For local Docker on the same machine, the browser should call the backend via localhost (because docker ports are published).
REACT_APP_API_URL=http://localhost:5000/entries

